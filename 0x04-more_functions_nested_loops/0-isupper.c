#include "main.h"
#include <ctype.h>
/**
 * _isupper - uppercase letters
 * @c: character to check
 *
 * Return: 0 or 1
 */

int _isupper(int c);
{
	if (isupper(c))
		return (1);
	else
		return (0);
}
